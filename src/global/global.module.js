import StorageService from './services/storage.service';

let GlobalModule = angular.module('GlobalModule', []);

GlobalModule.service('StorageService', StorageService);

export default GlobalModule = GlobalModule.name;